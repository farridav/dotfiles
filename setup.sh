#!/bin/bash

rm ~/.bashrc && ln -s .bashrc ~/.bashrc
rm ~/.bash_aliases && ln -s .bash_aliases ~/.bash_aliases
rm ~/.gitconfig && ln -s .gitconfig ~/.gitconfig
rm ~/.gitignore && ln -s .gitignore ~/.gitignore
rm ~/.noserc && ln -s .noserc ~/.noserc

